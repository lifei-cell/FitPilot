apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fitpilot
spec:
  rules:
    - host: api.fitpilot.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: {name: fitpilot, port: {name: http}}

