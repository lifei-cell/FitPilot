apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitpilot
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  minReadySeconds: 10
  progressDeadlineSeconds: 300

